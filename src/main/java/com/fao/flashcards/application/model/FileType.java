package com.fao.flashcards.application.model;

/**
 * Enum für verschiedene Dateitypen, die in Projekten hochgeladen werden können.
 */
public enum FileType {
    /**
     * Bilddateien (jpg, png, gif, etc.)
     */
    IMAGE,
    
    /**
     * Dokumentdateien (pdf, doc, txt, etc.)
     */
    DOCUMENT
}